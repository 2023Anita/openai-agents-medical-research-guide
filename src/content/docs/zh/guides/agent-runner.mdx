---
title: Agent + Runner
description: 定义谁来做，以及怎样把任务跑起来。
sidebar:
  order: 2
---

import InteractiveExercise from '../../../../components/InteractiveExercise.astro';

![Agent and Runner](../../../../assets/architect/01-agent.png)

## 一句话解释

`Agent` 定义“谁来做”，`Runner` 负责“把它跑起来”。

## 生活类比

`Agent` 像你请来的研究助理，`Runner` 像你把任务交给助理并等待结果。

## 最小代码

```python
from agents import Agent, Runner

agent = Agent(
    name="医疗科研助手",
    instructions="你只回答医学科研设计问题，不提供诊断或治疗建议。",
)

result = Runner.run_sync(
    agent,
    "SOFA 动态变化是否能预测脓毒症 ICU 患者 28 天死亡？",
)
print(result.final_output)
```

## 医疗科研场景

第一步先让它回答“这个研究问题应该怎么拆解”，不要一开始就让它做全套系统。基础 agent 稳定后，再逐步加工具、记忆、多 agent 和安全护栏。

## 常见坑

- `instructions` 写得像口号，结果输出不稳定。
- 没写医疗边界，模型容易滑向诊疗建议。
- 一开始就上多 agent，调试成本变高。

## 练习任务

<InteractiveExercise
  id={"zh-agent-runner-interactive-check"}
  kind={"single"}
  title={"角色边界：护理科研助手应该怎么写？"}
  prompt={"如果把 agent 改成“护理科研助手”，下面哪条 instructions 最稳妥？"}
  options={[
  {
    "id": "a",
    "label": "你是万能医疗助手，尽量回答所有患者和科研问题。"
  },
  {
    "id": "b",
    "label": "你可以根据患者情况给出护理诊断和治疗建议。"
  },
  {
    "id": "c",
    "label": "你只回答护理科研设计、量表选择和论文结构问题，不提供诊断、治疗、分诊或患者个体化建议。"
  },
  {
    "id": "d",
    "label": "你优先生成完整论文，不需要说明不确定性。"
  }
]}
  answers={["c"]}
  feedback={{
  "correct": "正确。Agent 的第一步是定角色和边界：护理科研可以做，患者个体化诊疗不能做。",
  "incorrect": "这条指令还不够安全。好的 Agent 指令要同时写清楚“能做什么”和“不能做什么”。",
  "required": "先选择一个答案，再检查。",
  "completed": "正确。Agent 的第一步是定角色和边界：护理科研可以做，患者个体化诊疗不能做。"
}}
  checkLabel={"检查答案"}
  resetLabel={"重来一次"}
  completedLabel={"已完成"}
  typeLabel={"单选题"}
  reviewNote={"这是科研学习练习，不构成临床诊疗建议；真实项目仍需研究者、统计师、伦理审查或临床专家复核。"}
  openPractice={"开放复刻任务：把示例 agent 改成护理科研助手，并保留同样的安全边界。"}
/>

---
title: Tools
description: 让 agent 调用 Python 函数完成实际任务。
sidebar:
  order: 3
---

import InteractiveExercise from '../../../../components/InteractiveExercise.astro';

![Tool flow](../../../../assets/architect/05-tool.png)

## 一句话解释

Tools 让 agent 不只是聊天，还能调用你写的 Python 函数。

## 生活类比

没有工具的助理只能凭记忆说话；有工具的助理可以查表、算数、读文件、格式化报告。

## 最小代码

```python
from agents import Agent, Runner, function_tool

@function_tool
def explain_scale(scale_name: str) -> str:
    """解释医学量表的科研用途。"""
    if scale_name.upper() == "SOFA":
        return "SOFA 常用于器官功能衰竭评估；科研中要明确评分时间点。"
    return "请确认量表适用人群、评分范围和验证版本。"

agent = Agent(
    name="量表解释助手",
    instructions="回答医学科研问题，需要时调用工具。",
    tools=[explain_scale],
)

result = Runner.run_sync(agent, "SOFA 在 ICU 研究中怎么用？")
print(result.final_output)
```

## 医疗科研场景

先写三类工具就够了：

- `search_literature(query)`：文献检索占位或真实数据库检索。
- `explain_scale(scale_name)`：解释 APACHE II、SOFA、Charlson 等量表。
- `format_research_brief(...)`：把输出固定成科研草案。

## 常见坑

- 工具 docstring 写得太模糊，模型不知道何时调用。
- 工具直接改数据库或发请求，没有确认机制。
- 工具返回太长文本，浪费上下文。

## 练习任务

<InteractiveExercise
  id={"zh-tools-interactive-check"}
  kind={"multiple"}
  title={"工具检查：explain_design() 至少要覆盖什么？"}
  prompt={"新增 `explain_design(design_type)` 时，下面哪些要求应该被视为通过练习的必要条件？"}
  options={[
  {
    "id": "a",
    "label": "能解释 cohort / 队列研究的适用场景。"
  },
  {
    "id": "b",
    "label": "能解释 case-control / 病例对照研究的适用场景。"
  },
  {
    "id": "c",
    "label": "能解释 cross-sectional / 横断面研究的适用场景。"
  },
  {
    "id": "d",
    "label": "能根据患者情况直接推荐治疗方案。"
  }
]}
  answers={["a","b","c"]}
  feedback={{
  "correct": "正确。这个工具要覆盖三种研究设计，并保持科研解释边界；它不应该变成治疗推荐工具。",
  "incorrect": "再检查一下：这个工具的目标是解释研究设计，而不是输出患者诊疗建议。三种设计都要覆盖。",
  "required": "先选择一个答案，再检查。",
  "completed": "正确。这个工具要覆盖三种研究设计，并保持科研解释边界；它不应该变成治疗推荐工具。"
}}
  checkLabel={"检查答案"}
  resetLabel={"重来一次"}
  completedLabel={"已完成"}
  typeLabel={"多选题"}
  reviewNote={"这是科研学习练习，不构成临床诊疗建议；真实项目仍需研究者、统计师、伦理审查或临床专家复核。"}
  openPractice={"开放复刻任务：实现 `explain_design(design_type)`，并为未知设计类型返回“请确认研究问题和数据来源”的提示。"}
/>

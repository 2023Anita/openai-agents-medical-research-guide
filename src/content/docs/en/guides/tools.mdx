---
title: Tools
description: Let the agent call Python functions.
sidebar:
  order: 3
---

import InteractiveExercise from '../../../../components/InteractiveExercise.astro';

![Tool flow](../../../../assets/architect/05-tool.png)

## One-sentence explanation

Tools let the agent call Python functions instead of only generating text.

## Analogy

Without tools, the assistant only speaks from memory. With tools, it can look things up, calculate, read files, and format reports.

## Minimal code

```python
from agents import Agent, Runner, function_tool

@function_tool
def explain_scale(scale_name: str) -> str:
    """Explain the research use of a clinical scale."""
    if scale_name.upper() == "SOFA":
        return "SOFA is used to assess organ dysfunction. Research protocols must define scoring time points."
    return "Confirm the target population, score range, interpretation, and validation version."

agent = Agent(
    name="Scale Explanation Assistant",
    instructions="Answer medical research questions and call tools when useful.",
    tools=[explain_scale],
)

result = Runner.run_sync(agent, "How is SOFA used in ICU research?")
print(result.final_output)
```

## Medical research use

Start with three tool types:

- `search_literature(query)`: placeholder or real literature search.
- `explain_scale(scale_name)`: APACHE II, SOFA, Charlson, and other scales.
- `format_research_brief(...)`: turn text into a reusable research brief.

## Common mistakes

- Tool docstrings are too vague.
- Tools change databases or send requests without confirmation.
- Tool output is too long and wastes context.

## Exercise

<InteractiveExercise
  id={"en-tools-interactive-check"}
  kind={"multiple"}
  title={"Tool check: what must explain_design() cover?"}
  prompt={"When adding `explain_design(design_type)`, which requirements should count as passing the exercise?"}
  options={[
  {
    "id": "a",
    "label": "Explain when cohort studies are useful."
  },
  {
    "id": "b",
    "label": "Explain when case-control studies are useful."
  },
  {
    "id": "c",
    "label": "Explain when cross-sectional studies are useful."
  },
  {
    "id": "d",
    "label": "Recommend treatment plans for individual patients."
  }
]}
  answers={["a","b","c"]}
  feedback={{
  "correct": "Correct. The tool should cover the three study designs and stay inside research explanation, not treatment advice.",
  "incorrect": "Check again: the tool explains research designs. It should not become a treatment recommendation tool.",
  "required": "Choose an answer before checking.",
  "completed": "Correct. The tool should cover the three study designs and stay inside research explanation, not treatment advice."
}}
  checkLabel={"Check answer"}
  resetLabel={"Try again"}
  completedLabel={"Completed"}
  typeLabel={"Multiple choice"}
  reviewNote={"This is a research-learning exercise, not clinical advice. Real projects still need human review by researchers, statisticians, ethics reviewers, or clinical experts."}
  openPractice={"Open practice: implement `explain_design(design_type)` and return a clarification prompt for unknown design types."}
/>
